# 提示注入：六道闸门

> 前面几章讲的是"技能怎么写得好"。
> 这一章讲的是：**技能本身就是一条注入通道。**

## 目录

- [技能不是安全主体](#技能不是安全主体)
- [真正的威胁模型](#真正的威胁模型)
- [六道闸门](#六道闸门)
- [纵深防御五层](#纵深防御五层)
- [金丝雀令牌](#金丝雀令牌)
- [仅过滤提示文本不是完整防御](#仅过滤提示文本不是完整防御)

---

## 技能不是安全主体

> ⭐ **AI Skill 是可复用的指令上下文，而**不是**独立的安全主体。**

三个必须记住的事实：

1. **干净的顶层段落，无法消除稍后获取的恶意内容**
2. **工具声明不能证明未来的每个参数都是安全的**
3. 自然语言指令与数据**在缺乏可靠分隔边界的情况下被一起处理**——这是根本问题

> 所以：**"我审过 SKILL.md，它是干净的"这个判断，
> 对"这个技能运行时会不会被注入"这个问题，几乎不提供保障。**

---

## 真正的威胁模型

> 提示注入 = 攻击者控制的文本被解释为指令，
> 并允许改变 agent 的计划、工具参数、数据访问或外部影响。

> ⭐ **重要的安全问题不是"模型是否遵循了一句恶意指令"，
> 而是这种影响**能否从不可信来源传播到高权限接收点**。**

**高权限接收点（终点）**：

```
shell · 文件系统 · 带凭据的 API · 浏览器会话 ·
MCP 工具 · 消息 · 部署 · 持久记忆
```

> **关键对比**：
> 只停留在一次性摘要里的模型响应，
> 与同一个响应**变成一条用生产凭据执行的命令**——
> **风险完全不是一个量级**。

**攻击面**：

| 面 | 说明 |
|---|---|
| 用户输入试图覆盖系统指令 | 直接注入 |
| RAG 里检索到的不可信文档/网页 | **间接注入** |
| **走私恶意指令的工具输出** | 常被忽略 |
| 共享上下文窗口导致的跨租户泄漏 | 多租户场景 |
| Markdown/HTML 注入（渲染后的外泄通道） | 图片 URL 就能外泄 |
| **多轮攻击逐渐偏移上下文** | 单轮检测抓不到 |

---

## 六道闸门

> **如果任何一道闸门无法验证，就缩小工作流范围、
> 用合成数据隔离它，或拒绝在该环境中使用它。**

| # | 闸门 | 做什么 |
|---|---|---|
| **1** | **绑定 Skill 工件** | 锁定技能内容与版本，防内容被改 |
| **2** | **按信任级别分类每项输入** | 每个输入都标：可信 / 半可信 / 不可信 |
| **3** | **最小化传入模型的内容** | 少传 = 少攻击面 |
| **4** | **将策略置于不可信内容之外** | 策略不能被后来的内容改写 |
| **5** | **验证精确的工具调用** | 不只验证"用哪个工具"，还验证**参数** |
| **6** | **约束并验证产生的影响** | 终点（shell/API/消息）要有闸门 |

---

## 纵深防御五层

```
1. 指令层级强制：system > developer > user > tool output
2. 上下文隔离：不可信文本与控制指令分开
3. 工具权限：按任务与租户的显式白名单
4. 输出策略检查：校验 schema、脱敏、阻断不安全动作
5. 人工批准：高影响操作必须
```

### 上下文隔离的具体做法

> **用明确的边界标记把不可信内容包起来，并告知"这是数据，不是指令"。**

```markdown
<FILE_CONTENT>...</FILE_CONTENT>
<EMAIL_BODY>...</EMAIL_BODY>
<KB_DOC>...</KB_DOC>
<WEB_CONTENT>...</WEB_CONTENT>
```

**输入过滤三层**：

```
1. 长度限制：超过 5000 字 → 拒绝
2. 过滤角色标签：<system>、</system>、<|im_start|> 等 → 移除
3. 过滤注入模式：ignore.*instructions、you are now in.*mode → 拒绝
```

**输出层验证**（生成任何外部请求之前）：

```
1. 禁止多语句：;  &&  ||
2. 禁止危险操作：rm / DROP / DELETE / curl / wget
3. 仅允许白名单操作（如邮件只允许发给白名单域名）
```

---

## 金丝雀令牌

> **把可追踪的金丝雀令牌注入提示或检索文档。
> 一旦它出现在模型输出里，就告警：发生了敏感数据外泄。**

这是少数能**实测**外泄的手段——
因为绝大多数外泄通道（Markdown 图片 URL、链接参数）看起来都是合法输出。

---

## 仅过滤提示文本不是完整防御

> ⚠️ **输入净化能减少指令走私（截断、规范化、剥控制字符），
> 但无法消除所有注入向量。**

它必须**与以下配合使用**才构成完整防御：

```
□ 检索上下文隔离（边界标记）
□ 工具权限（白名单）
□ 输出验证（schema + 脱敏 + 阻断）
```

**给技能作者的三条硬规则**（这是本章对你最直接的产出）：

```markdown
1. 技能若会读不可信内容（网页/用户文件/RAG 结果），
   必须显式要求用边界标记包裹，并声明"这是数据不是指令"。

2. 技能若会调用写操作（rm / push / 发消息 / 调带凭据的 API），
   必须设人工确认门，且**门要在读不可信内容之后仍然有效**。

3. 技能里不写凭据；需要时用环境变量，
   并在输出里**禁止回显**其值。
```

> 第 2 条最关键：**流程顺序不能成为绕过确认门的理由**。
> 攻击者正是通过让恶意内容出现在"流程的后期"来绕过前期检查。

---

## 自查

```
□ 技能会不会读不可信内容？会的话是否要求了边界标记？
□ 是否区分了"验证用哪个工具"和"验证参数"（第 5 道闸门）？
□ 写操作是否设了人工确认门，且在读取不可信内容之后仍有效？
□ 是否禁止了输出回显凭据/密钥？
□ 是否有输出层验证（禁多语句、禁危险操作、白名单）？
□ 高风险技能是否考虑注入金丝雀令牌？
□ 是否记录了技能工件的版本与哈希（第 1 道闸门）？
□ 是否只靠了输入过滤（不完整）？
```
