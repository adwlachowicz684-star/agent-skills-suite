# 每次手动修复都应该变成一条用例

> 相关：`skill-evaluating` 的 `trigger-eval-set.md` ·
> `regression-baseline.md` · `suite-maintenance.md`

---

## 目录

- [1. ⭐ 核心纪律](#1--核心纪律)
- [2. 四类用例与数量](#2-四类用例与数量)
- [3. 不需要大型基准集](#3-不需要大型基准集)
- [4. 官方用 evals.json，不是 CSV](#4-官方用-evalsjson不是-csv)
- [5. CSV 会变成活文档](#5-csv-会变成活文档)

---

## 1. ⭐ 核心纪律

> ⭐ **每一次手动修复，都可以成为未来评估用例。**

理由：

> **只有锁定预期行为，才能规模化评估。**

你手动修了一次，说明那个行为**曾经是错的**。
如果不在用例里锁住它，下次改动很可能又退回去。

---

## 2. 四类用例与数量

| 类型 | 测什么 | 数量 |
|---|---|---|
| **显式调用** | 直接点名技能，验证基础功能 | 5–8 |
| **隐式调用** | 只描述场景不提技能名，测 description 是否够清晰 | 5–8 |
| **上下文调用** | 加业务干扰，测是否仍正确触发 | 若干 |
| ⭐ **负向控制** | ⭐ 明确不该触发的场景，防误触发 | 3–5 |

> ⭐ **负样本是验证 description 是否过宽的唯一手段。**

```
遇到新失败案例就持续新增条目
```

---

## 3. 不需要大型基准集

> ⭐ **10–20 条提示就足够，用 CSV 管理即可。**

不要一开始就做几百条——维护成本会压垮你，
而 10–20 条已经能覆盖大多数回归。

---

## 4. 官方用 evals.json，不是 CSV

> ⚠️ **在 Claude 技能创建器里不需要这份 CSV**——
> 它使用 `evals/evals.json` 作为标准测试存储文件。

所以：

| 场景 | 格式 |
|---|---|
| 用官方 skill-creator | ⭐ `evals/evals.json` |
| 自建流程 | CSV（够用） |

---

## 5. CSV 会变成活文档

> ⭐ **这份 CSV 会成为技能必须通过的活文档。**

它不是一次性的测试脚本，而是**技能契约的可执行形式**——
每次改动都要过一遍，新失败不断加进去。

这与 `suite-maintenance.md` 那条呼应：
**100% 通过是坏消息**——说明你的用例没跟上技能的复杂度。

---

## 速查

| 事 | 做法 |
|---|---|
| 修了一个 bug | ⭐ 加一条用例 |
| 规模 | 10–20 条起步 |
| 负样本 | 3–5 条，必须有 |
| 官方工具 | evals/evals.json |
| 自建 | CSV 即可 |
| 用例全过 | ⭐ 警惕——说明用例太弱 |
